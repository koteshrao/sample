<%= form_for(@edu) do |f| %>
  <% if @edu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@edu.errors.count, "error") %> prohibited this emp from being saved:</h2>

      <ul>
      <% @edu.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course %><br />
    <%= f.text_field :course %>
  </div>
  <div class="field">
    <%= f.label :place %><br />
    <%= f.text_field :place %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
