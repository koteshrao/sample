<h1>Listing kotis</h1>

<table>
  <tr>
    <th>Place</th>
    <th>Course</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @kotis.each do |koti| %>
  <tr>
    <td><%= koti.place %></td>
    <td><%= koti.course %></td>
    <td><%= link_to 'Show', koti %></td>
    <td><%= link_to 'Edit', edit_koti_path(koti) %></td>
    <td><%= link_to 'Destroy', koti, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Koti', new_koti_path %>
